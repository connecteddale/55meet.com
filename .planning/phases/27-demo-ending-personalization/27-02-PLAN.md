---
phase: 27-demo-ending-personalization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/demo/synthesis.html
autonomous: true

must_haves:
  truths:
    - "Visitor sees 'imagine your team' challenge immediately after gap reveal"
    - "Challenge is visceral and personal with 'What would finding YOUR drag be worth?'"
    - "Challenge references specific quarter/time being lost"
    - "Challenge personalized based on gap type (Direction/Alignment/Commitment)"
    - "Email CTA links to connectedworld@gmail.com with pre-filled subject including gap type"
    - "Generic footer replaced with conversion-focused experience"
  artifacts:
    - path: "templates/demo/synthesis.html"
      provides: "Personalized conversion section replacing generic footer"
      contains: "connectedworld@gmail.com"
  key_links:
    - from: "templates/demo/synthesis.html"
      to: "synthesis_gap_type template variable"
      via: "Jinja2 conditional"
      pattern: "synthesis_gap_type.*Direction\\|Alignment\\|Commitment"
---

<objective>
Transform synthesis page footer into visceral personal challenge with gap-type personalized email CTA.

Purpose: The synthesis reveal is the peak emotional moment. Current generic footer with calendar link and restart demo dilutes conversion. Replace with personalized challenge that uses the gap type (already available in template context) to make conversion irresistible.

Output: synthesis.html with conversion section replacing generic footer.
</objective>

<execution_context>
@/home/DrPloy/.claude/get-shit-done/workflows/execute-plan.md
@/home/DrPloy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-demo-ending-personalization/27-RESEARCH.md

@templates/demo/synthesis.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace generic footer with personalized conversion section</name>
  <files>templates/demo/synthesis.html</files>
  <action>
Replace the entire footer section (lines 117-133) with a personalized conversion section.

**Current footer to remove:**
```html
<footer class="tools-footer">
    <div class="footer-links">
        <a href="https://calendar.app.google/omyaNipJt4E6cu1r9" target="_blank" rel="noopener" class="footer-link primary">
            Book your 1st session
        </a>
        <a href="https://www.connecteddale.com" target="_blank" rel="noopener" class="footer-link">
            More about Dale
        </a>
        <a href="/" class="footer-link">
            Back to top
        </a>
        <a href="/demo" class="footer-link">
            Restart demo
        </a>
    </div>
</footer>
```

**Replace with personalized conversion section:**
```html
<!-- Conversion Section - Personalized by Gap Type -->
<section class="demo-conversion-section">
    {% if synthesis_gap_type == 'Direction' %}
        <h2 class="conversion-headline">Imagine your team pulling in the same direction.</h2>
        <p class="conversion-challenge">What would finding YOUR direction drag be worth? A quarter? Two?</p>
        <p class="conversion-body">Right now, your team may lack shared understanding of goals or priorities. The 55 catches this drift before it compounds.</p>
    {% elif synthesis_gap_type == 'Alignment' %}
        <h2 class="conversion-headline">Imagine your team's work actually fitting together.</h2>
        <p class="conversion-challenge">What would finding YOUR alignment drag be worth? A quarter? Two?</p>
        <p class="conversion-body">Right now, your team's work may be disconnected or uncoordinated. The 55 catches this drift before it compounds.</p>
    {% elif synthesis_gap_type == 'Commitment' %}
        <h2 class="conversion-headline">Imagine everyone rowing the same boat.</h2>
        <p class="conversion-challenge">What would finding YOUR commitment drag be worth? A quarter? Two?</p>
        <p class="conversion-body">Right now, individual interests may be overriding collective success. The 55 catches this drift before it compounds.</p>
    {% else %}
        <h2 class="conversion-headline">Imagine finding your team's drag.</h2>
        <p class="conversion-challenge">What would that be worth? A quarter? Two?</p>
        <p class="conversion-body">The 55 catches alignment drift before it compounds.</p>
    {% endif %}

    <div class="conversion-cta-container">
        <a href="mailto:connectedworld@gmail.com?subject=About%20The%2055%20-%20{{ synthesis_gap_type }}%20Gap%20Demo" class="conversion-cta-primary">
            Email Dale about my {{ synthesis_gap_type }} gap
        </a>
        <p class="conversion-cta-helper">Dale facilitates The 55 monthly alignment diagnostic</p>
    </div>
</section>

<!-- Subtle secondary navigation -->
<footer class="demo-conversion-footer">
    <a href="/demo" class="conversion-link-subtle">Start a new demo</a>
</footer>
```

**Key requirements satisfied:**
- DEMO-22: "imagine your team" challenge immediately after gap reveal
- DEMO-23: Visceral personal challenge ("What would finding YOUR drag be worth?")
- DEMO-24: Quarter/time reference ("A quarter? Two?")
- DEMO-25: Personalized by gap type (Direction/Alignment/Commitment)
- DEMO-26: Email CTA to connectedworld@gmail.com with pre-filled subject including gap type
- Removes calendar link, "More about Dale", "Back to top"
- Keeps subtle "Start a new demo" link (Phase 26 research: keep restart but subtle)
  </action>
  <verify>
Run: `grep -c "connectedworld@gmail.com" /var/www/sites/55meet.com/templates/demo/synthesis.html`
Expected: 1

Run: `grep "synthesis_gap_type == 'Direction'" /var/www/sites/55meet.com/templates/demo/synthesis.html`
Expected: Match found

Run: `grep -c "calendar.app.google" /var/www/sites/55meet.com/templates/demo/synthesis.html`
Expected: 0 (calendar link removed)
  </verify>
  <done>
Generic footer replaced with gap-type personalized conversion section including:
- Personalized headline based on gap type
- "What would finding YOUR X drag be worth?" challenge
- "A quarter? Two?" time reference
- Email CTA with pre-filled subject
- Subtle restart demo link only
  </done>
</task>

<task type="auto">
  <name>Task 2: Add conversion section CSS styles</name>
  <files>templates/demo/synthesis.html</files>
  <action>
Add CSS styles for the new conversion section within the existing `<style>` block in synthesis.html.

**Add these styles after the existing `.tools-footer` styles (which can be removed or kept - the new classes override):**

```css
/* Conversion Section */
.demo-conversion-section {
    padding: var(--space-10) var(--space-5);
    text-align: center;
    background: linear-gradient(135deg, var(--color-bg-secondary) 0%, rgba(0, 102, 204, 0.05) 100%);
    border-radius: 12px;
    margin-bottom: var(--space-6);
}

.conversion-headline {
    font-size: var(--text-2xl);
    font-weight: 700;
    letter-spacing: var(--tracking-tight);
    margin-bottom: var(--space-3);
    color: var(--color-text);
}

.conversion-challenge {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-primary);
    margin-bottom: var(--space-4);
}

.conversion-body {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    max-width: 480px;
    margin: 0 auto var(--space-6);
    line-height: var(--leading-relaxed);
}

.conversion-cta-container {
    margin-top: var(--space-6);
}

.conversion-cta-primary {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    background: var(--color-primary);
    color: white;
    padding: var(--space-4) var(--space-8);
    border-radius: 8px;
    font-size: var(--text-lg);
    font-weight: 600;
    text-decoration: none;
    transition: background var(--duration-fast) var(--ease-out),
                box-shadow var(--duration-fast) var(--ease-out),
                transform var(--duration-fast) var(--ease-out);
}

.conversion-cta-primary:hover {
    background: var(--color-primary-dark);
    box-shadow: var(--shadow-md);
    transform: translateY(-1px);
    text-decoration: none;
}

.conversion-cta-helper {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    margin-top: var(--space-3);
}

/* Subtle footer for restart */
.demo-conversion-footer {
    padding-top: var(--space-8);
    text-align: center;
}

.conversion-link-subtle {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    text-decoration: none;
    padding: var(--space-2) var(--space-4);
    border-radius: 6px;
    transition: color var(--duration-fast) var(--ease-out);
}

.conversion-link-subtle:hover {
    color: var(--color-text-secondary);
    text-decoration: none;
}

/* Responsive */
@media (max-width: 640px) {
    .conversion-headline {
        font-size: var(--text-xl);
    }

    .conversion-challenge {
        font-size: var(--text-lg);
    }
}
```

Note: Remove or keep the old `.tools-footer` and `.footer-link` styles - they're no longer used but cause no harm. Cleaner to remove them.
  </action>
  <verify>
Run: `grep -c "conversion-cta-primary" /var/www/sites/55meet.com/templates/demo/synthesis.html`
Expected: At least 2 (one in HTML, one in CSS)

Run: `grep "demo-conversion-section" /var/www/sites/55meet.com/templates/demo/synthesis.html`
Expected: Match found (in both HTML and CSS)
  </verify>
  <done>
Conversion section styles added matching existing design system (CSS variables, Inter font, Apple colors).
  </done>
</task>

<task type="auto">
  <name>Task 3: Test personalization with all gap types</name>
  <files>templates/demo/synthesis.html</files>
  <action>
Verify the personalization works correctly by checking the template renders properly.

The demo uses a pre-baked synthesis with Alignment gap type (from DEMO_SYNTHESIS in demo.py), so production testing will show Alignment. However, verify the template structure is correct for all three gap types.

**Verification approach:**
1. Check template syntax is valid (no Jinja2 errors)
2. Check all three gap type conditionals are present
3. Check mailto link includes gap_type variable
4. Check page loads without 500 errors
  </action>
  <verify>
Run: `curl -s -o /dev/null -w "%{http_code}" http://localhost:8055/demo/synthesis`
Expected: 200 (or 302 redirect which is also valid for demo flow)

Run: `grep -c "synthesis_gap_type ==" /var/www/sites/55meet.com/templates/demo/synthesis.html`
Expected: 3 (Direction, Alignment, Commitment)

Run: `grep "mailto:connectedworld@gmail.com" /var/www/sites/55meet.com/templates/demo/synthesis.html`
Expected: Contains {{ synthesis_gap_type }}
  </verify>
  <done>
Template has correct Jinja2 conditionals for all three gap types (Direction, Alignment, Commitment) with proper fallback, and email CTA dynamically includes gap type in subject.
  </done>
</task>

</tasks>

<verification>
Overall plan verification:

1. Synthesis page loads:
   `curl -s -o /dev/null -w "%{http_code}" http://localhost:8055/demo/synthesis`
   Expected: 200 or valid redirect

2. Calendar link removed:
   `curl -s http://localhost:8055/demo/synthesis | grep -c "calendar.app.google"`
   Expected: 0

3. Email CTA present:
   `curl -s http://localhost:8055/demo/synthesis | grep "connectedworld@gmail.com"`
   Expected: Match found with mailto link

4. Personalized challenge present (Alignment is the demo gap type):
   `curl -s http://localhost:8055/demo/synthesis | grep "fitting together\|alignment drag"`
   Expected: Match found

5. Restart demo link present (subtle):
   `curl -s http://localhost:8055/demo/synthesis | grep "Start a new demo"`
   Expected: Match found
</verification>

<success_criteria>
- DEMO-22 satisfied: "imagine your team" challenge replaces generic footer
- DEMO-23 satisfied: Visceral personal challenge present
- DEMO-24 satisfied: Quarter/time reference included
- DEMO-25 satisfied: Gap type personalization with three conditionals
- DEMO-26 satisfied: Email CTA to connectedworld@gmail.com with pre-filled subject
- Calendar link removed (no competing CTA)
- Restart demo link kept but subtle (not competing with primary CTA)
- Design matches existing system (CSS variables, typography)
</success_criteria>

<output>
After completion, create `.planning/phases/27-demo-ending-personalization/27-02-SUMMARY.md`
</output>
