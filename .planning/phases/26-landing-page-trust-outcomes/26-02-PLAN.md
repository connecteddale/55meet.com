---
phase: 26-landing-page-trust-outcomes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/landing.html
  - static/css/landing.css
autonomous: true

must_haves:
  truths:
    - "Visitor sees 'What finding the drag looks like' section heading"
    - "Visitor sees 3 client example cards with before/after transformations"
    - "Each card shows gap type (Direction or Alignment)"
    - "Cards display in 3-column grid on desktop, stack on mobile"
    - "Card design matches existing Apple-esque design system"
  artifacts:
    - path: "templates/landing.html"
      provides: "Examples section with 3 client cards"
      contains: "What finding the drag looks like"
    - path: "static/css/landing.css"
      provides: "Example card grid and card styles"
      contains: "landing-examples-grid"
  key_links:
    - from: "templates/landing.html"
      to: "static/css/landing.css"
      via: "CSS class references"
      pattern: "landing-example-card"
---

<objective>
Add "What finding the drag looks like" section with 3 concrete client example cards to landing page.

Purpose: Visitors need to see concrete examples of outcomes before clicking the demo CTA. Research shows social proof before CTA increases conversion by 68%. Three client vignettes show the before/after transformation from each gap type.

Output: New #examples section in landing.html with 3 client cards, supporting CSS styles in landing.css.
</objective>

<execution_context>
@/home/DrPloy/.claude/get-shit-done/workflows/execute-plan.md
@/home/DrPloy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@templates/landing.html
@static/css/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add example cards CSS to landing.css</name>
  <files>static/css/landing.css</files>
  <action>
Add CSS for the examples section at the end of static/css/landing.css, before the closing comment or at the very end.

The styles should follow the existing landing.css patterns:
- Use existing CSS variables (--space-*, --text-*, --color-*, etc.)
- Follow .landing-contrast-grid pattern for the 3-column grid
- Match card styling to existing landing page aesthetic (Inter font, Apple colors)
- Stack to single column on mobile (640px breakpoint)

Add these styles:

```css
/* ========================================
   Client Examples Section
   ======================================== */

.landing-examples-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-6);
  max-width: 900px;
  margin: 0 auto;
  text-align: left;
}

.landing-example-card {
  background: var(--color-bg);
  border: 1px solid var(--color-border-light);
  border-radius: 12px;
  padding: var(--space-6);
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
}

.landing-example-label {
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--color-text-tertiary);
  margin: 0;
}

.landing-example-title {
  font-size: var(--text-lg);
  font-weight: 600;
  color: var(--color-text);
  margin: 0;
  line-height: var(--leading-snug);
}

.landing-example-transform {
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}

.landing-example-before,
.landing-example-after {
  font-size: var(--text-sm);
  line-height: var(--leading-relaxed);
  margin: 0;
  padding-left: var(--space-4);
  position: relative;
}

.landing-example-before::before,
.landing-example-after::before {
  position: absolute;
  left: 0;
  font-size: var(--text-xs);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.landing-example-before {
  color: var(--color-text-secondary);
}

.landing-example-before::before {
  content: "Before";
  color: var(--color-text-tertiary);
}

.landing-example-after {
  color: var(--color-text);
  font-weight: 500;
}

.landing-example-after::before {
  content: "After";
  color: var(--color-primary);
}

.landing-example-gap {
  font-size: var(--text-xs);
  font-weight: 600;
  color: var(--color-primary);
  background: var(--color-primary-light);
  padding: var(--space-1) var(--space-2);
  border-radius: 4px;
  align-self: flex-start;
  margin-top: auto;
}

@media (max-width: 640px) {
  .landing-examples-grid {
    grid-template-columns: 1fr;
    gap: var(--space-4);
  }

  .landing-example-card {
    padding: var(--space-5);
  }
}
```

Note: The Before/After labels use ::before pseudo-elements for clean semantic markup while maintaining the visual hierarchy.
  </action>
  <verify>
    grep -n "landing-examples-grid" static/css/landing.css && grep -n "landing-example-card" static/css/landing.css
  </verify>
  <done>
    CSS file contains .landing-examples-grid and .landing-example-card classes with responsive styles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add examples section to landing.html</name>
  <files>templates/landing.html</files>
  <action>
Insert a new #examples section in templates/landing.html AFTER the #evidence section (around line 47) and BEFORE the #drift section.

Find the closing of the #evidence section:
```html
        </section>

        <!-- Drift -->
```

Insert this new section between them:

```html
        <!-- Examples -->
        <section id="examples" class="landing-section landing-muted">
            <div class="landing-content reveal">
                <h2 class="landing-section-headline">What finding the drag looks like</h2>
                <div class="landing-examples-grid">
                    <div class="landing-example-card">
                        <p class="landing-example-label">Asset Manager</p>
                        <h3 class="landing-example-title">Geographic Expansion</h3>
                        <div class="landing-example-transform">
                            <p class="landing-example-before">Unclear on strategy for geographic expansion</p>
                            <p class="landing-example-after">Senior team fully aligned on expansion strategy</p>
                        </div>
                        <span class="landing-example-gap">Direction Gap</span>
                    </div>
                    <div class="landing-example-card">
                        <p class="landing-example-label">EdTech Company</p>
                        <h3 class="landing-example-title">Priority Alignment</h3>
                        <div class="landing-example-transform">
                            <p class="landing-example-before">Disconnected priorities across divisions</p>
                            <p class="landing-example-after">Clear business-wide priorities with divisional alignment</p>
                        </div>
                        <span class="landing-example-gap">Alignment Gap</span>
                    </div>
                    <div class="landing-example-card">
                        <p class="landing-example-label">Professional Services Firm</p>
                        <h3 class="landing-example-title">Growth Strategy</h3>
                        <div class="landing-example-transform">
                            <p class="landing-example-before">Uncertain how to grow as part of international network</p>
                            <p class="landing-example-after">Identified strategic levers for expansion</p>
                        </div>
                        <span class="landing-example-gap">Direction Gap</span>
                    </div>
                </div>
            </div>
            <a href="#drift" class="landing-scroll-cue" aria-label="Scroll down">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
            </a>
        </section>
```

Also update the scroll-cue link in the #evidence section to point to #examples instead of #drift:
- Change `href="#drift"` to `href="#examples"` in the evidence section's scroll cue.
  </action>
  <verify>
    grep -n "What finding the drag looks like" templates/landing.html && grep -n "landing-examples-grid" templates/landing.html && grep -n 'href="#examples"' templates/landing.html
  </verify>
  <done>
    Landing page contains #examples section with 3 client cards, evidence section links to examples, examples section links to drift.
  </done>
</task>

</tasks>

<verification>
1. Landing page loads without CSS errors
2. Examples section visible between Evidence and Drift sections
3. 3 client cards display in grid on desktop (>640px)
4. Cards stack vertically on mobile (<=640px)
5. Scroll navigation flows: Evidence -> Examples -> Drift
6. Card styling matches existing landing page aesthetic

Manual verification:
- Visit / on desktop - confirm 3-column grid
- Resize to mobile width - confirm single column stack
- Scroll through page - confirm smooth scroll-snap to examples section
- Check card hover states if any (none defined, cards are static)
</verification>

<success_criteria>
- [ ] `grep -n "landing-examples-grid" static/css/landing.css` returns matches
- [ ] `grep -n "What finding the drag looks like" templates/landing.html` returns match
- [ ] `grep -c "landing-example-card" templates/landing.html` returns 3 (three cards)
- [ ] Page loads without console errors
- [ ] Mobile view (640px) shows stacked cards
- [ ] Desktop view shows 3-column grid
</success_criteria>

<output>
After completion, create `.planning/phases/26-landing-page-trust-outcomes/26-02-SUMMARY.md`
</output>
