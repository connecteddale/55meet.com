---
phase: 26-landing-page-trust-outcomes
plan: 03
type: execute
wave: 2
depends_on: ["26-02"]
files_modified:
  - templates/landing.html
  - static/css/landing.css
autonomous: true

must_haves:
  truths:
    - "Visitor sees benefit-focused outcomes section with specific outcomes"
    - "Outcomes mention: faster execution, clearer priorities, less wasted work"
    - "Final CTA uses headline 'You've felt the drag. Now find it.'"
    - "Demo button is prominent primary action"
    - "Secondary email link to connectedworld@gmail.com exists"
  artifacts:
    - path: "templates/landing.html"
      provides: "Outcomes section and enhanced CTA section"
      contains: "faster execution"
    - path: "static/css/landing.css"
      provides: "Outcomes list styling and secondary link styling"
      contains: "landing-outcomes"
  key_links:
    - from: "templates/landing.html"
      to: "mailto:connectedworld@gmail.com"
      via: "secondary CTA link"
      pattern: "mailto:connectedworld@gmail\\.com"
---

<objective>
Add benefit-focused outcomes section and enhance the final CTA with "You've felt the drag. Now find it" messaging.

Purpose: Visitors need to understand what changes after finding the drag before they take action. The final CTA should be emotionally resonant and action-oriented, with demo as primary action and email as secondary.

Output: New #outcomes section before CTA, enhanced #cta section with strengthened copy and secondary email link.
</objective>

<execution_context>
@/home/DrPloy/.claude/get-shit-done/workflows/execute-plan.md
@/home/DrPloy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@templates/landing.html
@static/css/landing.css
@.planning/phases/26-landing-page-trust-outcomes/26-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add outcomes section CSS to landing.css</name>
  <files>static/css/landing.css</files>
  <action>
Add CSS for the outcomes section at the end of static/css/landing.css (after the Client Examples Section added in Plan 02).

The styles should follow existing landing.css patterns:
- Use existing CSS variables
- Match typography hierarchy (landing-section-headline, landing-body)
- Center-aligned list with subtle styling

Add these styles:

```css
/* ========================================
   Outcomes Section
   ======================================== */

.landing-outcomes-list {
  list-style: none;
  padding: 0;
  margin: var(--space-8) auto 0;
  max-width: 480px;
  text-align: left;
}

.landing-outcomes-list li {
  font-size: var(--text-lg);
  color: var(--color-text);
  padding: var(--space-3) 0;
  padding-left: var(--space-6);
  position: relative;
  line-height: var(--leading-relaxed);
}

.landing-outcomes-list li::before {
  content: "";
  position: absolute;
  left: 0;
  top: 1.1em;
  width: 8px;
  height: 8px;
  background: var(--color-primary);
  border-radius: 50%;
}

/* ========================================
   Enhanced CTA Section
   ======================================== */

.landing-cta-secondary {
  display: block;
  margin-top: var(--space-4);
  font-size: var(--text-sm);
  color: var(--color-text-tertiary);
}

.landing-cta-secondary a {
  color: var(--color-text-secondary);
  text-decoration: underline;
  text-underline-offset: 2px;
}

.landing-cta-secondary a:hover {
  color: var(--color-primary);
}

@media (max-width: 640px) {
  .landing-outcomes-list li {
    font-size: var(--text-base);
  }
}
```
  </action>
  <verify>
    grep -n "landing-outcomes-list" static/css/landing.css && grep -n "landing-cta-secondary" static/css/landing.css
  </verify>
  <done>
    CSS file contains .landing-outcomes-list and .landing-cta-secondary classes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add outcomes section and enhance CTA in landing.html</name>
  <files>templates/landing.html</files>
  <action>
Make two changes to templates/landing.html:

**Change 1: Add #outcomes section**

Insert a new #outcomes section AFTER the #facilitator section and BEFORE the #contrast section.

Find this pattern in the file:
```html
            </div>
            <a href="#contrast" class="landing-scroll-cue" aria-label="Scroll down">
```

This is the end of the #facilitator section. Insert the new #outcomes section just before the #contrast section comment:

```html
        <!-- Outcomes -->
        <section id="outcomes" class="landing-section">
            <div class="landing-content reveal">
                <h2 class="landing-section-headline">What changes after<br>you find the drag</h2>
                <ul class="landing-outcomes-list">
                    <li><strong>Faster execution</strong> — the team moves as one, not five people pulling in slightly different directions</li>
                    <li><strong>Clearer priorities</strong> — decisions get made because everyone agrees what matters most</li>
                    <li><strong>Less wasted work</strong> — no more building the wrong thing or solving yesterday's problem</li>
                </ul>
            </div>
            <a href="#contrast" class="landing-scroll-cue" aria-label="Scroll down">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
            </a>
        </section>
```

Also update the scroll-cue link in the #facilitator section to point to #outcomes instead of #contrast:
- Change `href="#contrast"` to `href="#outcomes"` in the facilitator section's scroll cue.

**Change 2: Enhance the #cta section**

Find the existing CTA section (around line 156-162 in original, may have shifted):
```html
        <!-- CTA -->
        <section id="cta" class="landing-section">
            <div class="landing-content reveal">
                <h2 class="landing-section-headline">You've felt the drag.<br>Now find it.</h2>
                <a href="/demo" class="btn btn-primary btn-large landing-cta">See how it works</a>
            </div>
        </section>
```

Replace with enhanced version:
```html
        <!-- CTA -->
        <section id="cta" class="landing-section">
            <div class="landing-content reveal">
                <h2 class="landing-section-headline">You've felt the drag.<br>Now find it.</h2>
                <a href="/demo" class="btn btn-primary btn-large landing-cta">Try the Demo</a>
                <p class="landing-cta-secondary">Or <a href="mailto:connectedworld@gmail.com?subject=About%20The%2055%20for%20my%20team">email Dale</a> to learn more</p>
            </div>
        </section>
```

Changes made:
1. Button text "See how it works" → "Try the Demo" (more action-oriented)
2. Added secondary email link with pre-filled subject line
3. The headline was already "You've felt the drag. Now find it." per LAND-08 - verify this is present, do not change if already correct
  </action>
  <verify>
    grep -n "What changes after" templates/landing.html && grep -n "faster execution" templates/landing.html && grep -n "mailto:connectedworld@gmail.com" templates/landing.html && grep -n "Try the Demo" templates/landing.html
  </verify>
  <done>
    Landing page contains #outcomes section with benefit language, CTA section has "Try the Demo" button and secondary email link to connectedworld@gmail.com.
  </done>
</task>

</tasks>

<verification>
1. Landing page loads without errors
2. Outcomes section visible between Facilitator and Contrast sections
3. Outcomes list shows: faster execution, clearer priorities, less wasted work
4. CTA headline reads "You've felt the drag. Now find it."
5. Demo button is primary action with "Try the Demo" text
6. Email link opens mail client with pre-filled subject
7. Scroll navigation flows correctly through all sections

Manual verification:
- Visit / and scroll through full page
- Verify outcomes section appears in correct position
- Click "Try the Demo" - should navigate to /demo
- Click "email Dale" - should open mail client with subject "About The 55 for my team"
- Test mobile view - outcomes list should be readable
</verification>

<success_criteria>
- [ ] `grep -n "landing-outcomes-list" static/css/landing.css` returns matches
- [ ] `grep -n "What changes after" templates/landing.html` returns match
- [ ] `grep -n "Faster execution" templates/landing.html` returns match (case-insensitive)
- [ ] `grep -n "mailto:connectedworld@gmail.com" templates/landing.html` returns match
- [ ] `grep -n "Try the Demo" templates/landing.html` returns match
- [ ] Page loads without console errors
- [ ] Email link has correct pre-filled subject
</success_criteria>

<output>
After completion, create `.planning/phases/26-landing-page-trust-outcomes/26-03-SUMMARY.md`
</output>
